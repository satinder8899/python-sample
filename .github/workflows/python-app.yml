# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Workflow for GitHub Advanced Security
on: push
jobs:
  sonatype-cli:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      # some steps are omitted...
      # Check out your code
      - name: Checkout
        id: checkout
        uses: actions/checkout@v4
      # Perform an evaluation
      - name: Run evaluate  action
        id: evaluate
        uses: sonatype/actions/evaluate@v1
        with:
          iq-server-url: https://nexus-iq-nexus-lifecycle.apps.ocp5.azure.dso.digital.mod.uk
          username: yEJlbq4C
          password: KRIy1OuKrPAH76AoAZFJpmc1xh4HgTuFhybIhAlNJetE
          application-id: lifecycle-app
          scan-targets: requirements.txt
          upload-sarif-file: true
